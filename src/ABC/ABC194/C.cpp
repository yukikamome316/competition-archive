#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < n; i++)
using namespace std;

int main() {
    int N;
    cin >> N;
    vector<int> data(N);
    rep(i, N) {
        cin >> data[i];
    }
    int idx = 0;
    for (int j = 1; j < idx - 1; j++) {
        for (int i = 2; i < N; i++) {
            idx = 
        }
    }

}