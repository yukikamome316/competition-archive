#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < n; i++)

using namespace std;

enum class FT : int {
    none = 0,
    left = 1,
    bottom = 2
};

int main() {
    while (true) {
        /*
         * width: 紙の幅
         * height: 紙の高さ
         * t: 折る回数
         * p: 穴を開ける回数
         */
        int width, height, t, p;
            cin >> width >> height >> t >> p;
        if (width == 0 && height == 0
            && t == 0 && p == 0) break;

        vector grid(height, vector<int>)

        
    }
    
    
}